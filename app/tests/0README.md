# Adresář /app/tests

Tento adresář obsahuje automatizované testy pro Flask backend aplikaci.

## Obsah
* `test_search.py` - Testovací skript (unittest) ověřující správnost fungování vyhledávání a deduplikace záznamů podle názvu (např. otestování dotazu na "Energetický zákon").

# Adresář /db

Tento adresář obsahuje vše potřebné pro sestavení a běh SQLite databáze. Nachází se zde inicializační skript (`init_db.py`), samotný výsledný databázový soubor (`regulatory_documents.db`) i UML designy či Enterprise Architect projekt a skripty s nimi spojené.

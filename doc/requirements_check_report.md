# Requirements compliance report

**Generated:** 2026-09-16 11:41 · **Commit:** `c5d40d6` + substantial uncommitted working-tree changes (repo is mid-reorganization; `git status` shows dozens of modified/deleted/untracked paths — this is **not** a clean-commit snapshot, treat the numbers below as "current working tree", not "as of c5d40d6")
**Against:** `doc/REQUIREMENTS.md` (dated 2026-09-11 15:37, unchanged text since last run)

Produced by the `requirements-check` agent (mechanical evidence from
`.venv/bin/python src/tools/check_requirements.py`, cross-referenced against
`app/app.py`, `app/templates/*.html`, `doc/konsolidace/*.sql`, `doc/PLAN.md`
§16, `wsgi.py`). This file is **overwritten** on every run — it is a
current snapshot, not an accumulating log. `check_requirements.py` was
substantially extended today (per-document R1.6 coverage, R1.7 field
coverage, R1.3/R1.4 ENUM-vs-usage, a fixed R2.3 probe) — several verdicts
below changed as a direct result of seeing facts the previous run's
checker could not see.

Full test suite: `580 passed` (`.venv/bin/python -m pytest tests/ -q`),
up from 467 at the last report.

## R1.x Data Architecture & Management

| Req | Verdict | Evidence | Gap |
|---|---|---|---|
| R1.1 Record uniqueness | PARTIAL | `Document.identifier` has a real `UNIQUE` constraint. 1182/1238 documents have a non-NULL identifier — **56 do not** (up from 46/1211 at the last report; the corpus grew and some new/synthesized records, e.g. ČSN-adoption rows from §9, still lack one). | MariaDB allows unlimited NULLs under a UNIQUE constraint, so those 56 rows physically bypass the uniqueness guarantee — the mechanism does not "physically prevent" every duplicate case as literally worded. Not a regression, but not shrinking either; worth a follow-up pass identifying *which* 56 and whether any are true duplicates hiding behind NULL. |
| R1.2 Jurisdictional tiering (int'l / EU / national) | PASS | `Document.jurisdikce_uroven ENUM('mezinárodní','EU','národní')`, generated from `jurisdikce`. Distribution: `národní` 820, `mezinárodní` 195, `EU` 70, `NULL` 153 (unclassified `jurisdikce='neurčeno'`/blank residual, honestly left out of the three tiers rather than guessed). | The `NULL` residual (153, ~12 % of the corpus) is real and unclassified — acceptable per the original design rationale (don't force-guess), but worth tracking if it grows. |
| R1.3 EU→national transposition links | PARTIAL | `document_relation`: `IMPLEMENTS` ×6, `AMENDS` ×1 (relation_type ENUM also declares `REPEALS`, `CONSOLIDATES` — both **declared but present with 0 rows**). Mechanism (`link_document_relations_auto.py`) is real and was previously verified end-to-end for its 6 targets. | 6 `IMPLEMENTS` edges against a corpus with 39 `Zákon` + 13 `Vyhláška` + 6 `Nařízení vlády` national instruments and 26+13+5 EU acts is thin coverage — plausible that many more national acts transpose an EU directive than are currently linked. `REPEALS`/`CONSOLIDATES` are modeled in the schema but never populated, so version supersession (R1.5) and relation modeling (R1.3) don't yet talk to each other. |
| R1.4 Standard localization linking (ISO ↔ ČSN EN ISO) | PASS | `NATIONAL_EQUIVALENT` ×14, `ADOPTS` ×25 (up from 15 at the last report, per §9/§16 work) — genuinely nonzero, so international standards and their national adoptions are demonstrably linked, not merely "supportable in principle." | Still a minority of the corpus: 1104 `Norma` records exist and the vast majority of STN/ČSN/DIN adoptions still lack a collected international-parent `Document` to link to at all — a data-collection gap, not a mechanism gap (same conclusion as before, updated numbers). |
| R1.5 Lifecycle & version control (active/superseded/draft) | PASS | `DocumentVersion.lifecycle_state ENUM('active','superseded','draft')`, populated: `active` 1085, `draft` 153, `superseded` 20 (1258 rows total). **Newly visible to users, not just in the DB**: `GET /dokument/<slug>` (`app/app.py:398`, `app/templates/document.html:80-98`) renders the full version table (version, edition, effective date, lifecycle state, current-version highlight) per document — previously this was populated but only ever read for an aggregate count. | None structural. The `superseded` count (20) is small relative to 1258 versions — plausible given how young the pipeline's supersession detection is, but not independently cross-checked against `REPEALS` relations (which are 0, see R1.3) so "document X supersedes document Y" isn't yet cross-linked between the two mechanisms. |
| R1.6 Authoritative source identification | PASS *(repaired 2026-09-16 after a same-day regression — see doc/PLAN.md §16.3/16.4)* | `DocumentSource` is an independent table, 59 rows (was 19 at the trough, 40 at the last report). **Real per-document coverage, not just table existence**: 1167/1238 (94.3 %) documents have a `source_id`, vs. 108/1238 (8.7 %) immediately after the §16.3 "Gestor must be one institution" change collapsed multi-institution lists too aggressively. Per-type breakdown: `Norma` 1064/1104, `Zákon` 39/39, `Směrnice EU` 13/13, `Nařízení vlády` 6/6, `Nařízení EU` 25/26, `Vyhláška` 9/13, `Rozhodnutí EU` 5/5, `Nezařazeno` 6/9, **`Bibliografický pramen` 0/23**. 40 distinct standard-setting bodies identified among the `Norma` sources. | `Bibliografický pramen` (23 records) has **zero** source coverage — every other type is at 55%+ and most are 90-100%. Also 40 `Norma` records (1104-1064) remain unresolved by design (no recognisable designation prefix, or a deliberately-unrecognised one — `MB`, `SEP`, `AR`, `TPP`, …, per PLAN §16.4) rather than guessed. This requirement had a real, undetected regression less than 24h before this report (108/1238) that the *previous* checker (row-count-only) could not have caught — flagging the punch-list item below to keep the new coverage probe in place permanently. |
| R1.7 General metadata incl. file paths | PARTIAL | `description` 780/1238 (63 %), `language` 1238/1238 (100 %, was ~5 % before §16.1), `identifier` 1182/1238 (95 %), `slug` 1238/1238 (100 %, new column). `file_path` 70/1238 (5.7 %) — unchanged rationale (only locally-cached copies get one, not a gap by itself). **450/1238 (36 %) flagged `needs_review`**, overwhelmingly (388+36=424) for missing description. | Description coverage (63 %) is the weakest single metadata field the requirement names explicitly ("titles, descriptions"), and over a third of the corpus is self-flagged as needing review — real and disclosed, not hidden, but it means "comprehensive metadata for all documents" is not yet true for descriptions specifically. Kept at PARTIAL rather than PASS (previous report's verdict) because the newly-visible field-coverage numbers make the description gap concrete rather than assumed-fine. |
| R1.8 Thematic indexing (M:N keywords) | PASS | `Keyword` 226 rows, `DocumentKeyword` (M:N) 3231 rows — no duplication mechanism, standard join table. | — |
| R1.9 Automated/semi-automated maintenance | PASS | Pipeline present and exercised this session: `build_unified_db.py`, `deduplicate_db.py`, `link_document_versions.py`, `init_db.py`, `load_document_relations.py`, `load_process_layer.py`, plus today's new `language.py`/`backfill_language.py`, `norm_title.py`/`backfill_norm_designation.py`, `puvodce.py`/`backfill_puvodce.py`, `standards_body.py`/`backfill_standards_body.py`, `slug.py` — all idempotent, all in `src/tools/`. | — |

## R2.x Search & Functional Capabilities

| Req | Verdict | Evidence | Gap |
|---|---|---|---|
| R2.1 Search result uniqueness | PASS | `app/app.py`: `base_query += " GROUP BY d.id ORDER BY d.title ASC"` (list query) and a separate `COUNT(DISTINCT d.id)` for the pagination total (`app/app.py:121,229`) — the same "keyword-join fan-out" trap R2.1 was fixed for once (commit `ebc1f60`) was proactively guarded against again for the new pager total, per `doc/PLAN.md` §16.7's own note. | — |
| R2.2 Responsive web UI | PASS | Viewport meta tag present (`base.html`); 3 real `@media` breakpoints in `style.css` (`992px`, `768px` ×2). | — |
| R2.3 Combined full-text + structured filters | PASS *(checker's own probe was corrected today)* | Filter parsing moved into `parse_filters(args)` (not directly in the route handler as the old grep assumed) — the new probe matches both shapes and confirms all four facets are parsed: `q`, `type_id`, `source_id`, `keyword_id`, ANDed onto one query. | — |
| R2.4 Result metadata summary + hyperlink | PASS | List view (`index.html`) still renders type/title/language/keywords/source/date/description per row with a conditional `doc.url` hyperlink. **Now additionally**: `GET /dokument/<slug>` (new today) gives each result a permanent detail page — full metadata (Gestor, jurisdikce + tier, identifier, effective date, permalink slug), an external-source link, a full-text download link gated by R4.1, the version-history table, and related documents in both relation directions. | — |
| R2.5 Export readiness (CSV/JSON/XML) | PASS | `/export/<fmt>` (csv/json/xml) in `app/app.py`, metadata-only fields, exports the full filtered set (not paginated/truncated at 50/page), 3 links in `index.html`. Unchanged since last report. | — |

## R3.x Technical Stack

| Req | Verdict | Evidence | Gap |
|---|---|---|---|
| R3.1 Python + Flask backend | PASS | `from flask import Flask, render_template, request, g, send_file, abort, Response, url_for` (`app/app.py:12`). | — |
| R3.2 Relational DB (MariaDB) | PASS | `doc/REQUIREMENTS.md` says MariaDB; `.env` → `localhost:3306` via `pymysql`, matches. Doc and reality agree (fixed in a prior run). | — |
| R3.3 HTML5/CSS3/Jinja2 | PASS | Templates: `base.html`, `document.html` (new), `index.html`; Jinja2 syntax confirmed in `index.html`. | — |
| R3.4 Vanilla JS + Phosphor Icons + Google Fonts | PASS | No `.js` files under `app/static`, no heavy JS framework references; Phosphor Icons + Google Fonts referenced in `base.html`. | — |

## R4.x Access Control & Licensing

| Req | Verdict | Evidence | Gap |
|---|---|---|---|
| R4.1 Licensing/visibility rules (metadata public, full text restricted) | PASS | `DocumentType.restricted_fulltext` (`TRUE` only for `Norma`); `/fulltext/<id>` route checks it fresh per-request (404/403/`send_file`). Real distribution of documents *with* a `file_path`: 63 unrestricted, 7 restricted (correctly gated). `document.html`'s new full-text link reuses the same server-side gate rather than trusting the template's own conditional (`app/templates/document.html:68-76`). | — |
| R4.2 Central public hub for stakeholders | N/A | Still not mechanically checkable. `PROJECT.md:8` states the intent explicitly: "strukturovaným a přístupným zdrojem informací pro všechny zainteresované subjekty" (a structured, accessible source of information for all interested parties) and names national/international norms as in scope. The app requires no login and serves all metadata publicly, consistent with that framing. `PROJECT.md:50` describes running it via `python app.py` on local port 5000 — there is no evidence in the repo of an actual public deployment (hosting, domain, access from outside a developer machine), so whether it currently *functions* as a hub reaching "state administration, industrial manufacturers, transport operators, research institutions" cannot be verified from the codebase. | — |

## Other findings (not tied to a specific requirement)

- **`wsgi.py` is correct** (`from app.app import app`) — this was fixed in a prior run (commit `ebc1f60`) and remains correct today. Note: `CLAUDE.md`'s own directory table still describes it as "aktuálně importuje `Web.app`" — that line in `CLAUDE.md` is now stale documentation (a doc-vs-reality mismatch worth fixing in `CLAUDE.md` itself, though that file is out of this report's write scope).
- **No duplicate `Web.zip` archives found** (`find . -iname "*.zip" -not -path "./zip/*"` returns nothing) — resolved in a prior run, still resolved.
- **`REPEALS`/`CONSOLIDATES` relation types are declared in the schema ENUM but have zero rows** (see R1.3 above) — either the pipeline needs a pass to populate them from real supersession events, or the ENUM should be trimmed to only what's actually produced; worth a project-owner call either way.
- **`Bibliografický pramen` (23 documents) has 0/23 source coverage** — the one `DocumentType` with no authoritative-source linkage at all (see R1.6 above); likely because these are literature/bibliographic references rather than legal instruments with an "issuing body" in the usual sense, but that's an assumption, not confirmed in code or docs.
- **Repo working tree is mid-reorganization with a large uncommitted diff** (per `git status`, dozens of `M`/`D`/`??` paths spanning `app/`, `data/`, `doc/`, `src/`, `tests/`, plus new untracked `Databaze/`, `Web/`, `src/standards/`). None of this blocks the verdicts above (the checker and this report read the live working tree, not the last commit), but it means "commit `c5d40d6`" alone does not reproduce the state this report describes — a `git status`/diff timestamp matters more than the hash right now.
- **The `check_requirements.py` extension itself is worth preserving going forward**: today's per-document R1.6 coverage query is what caught a same-day regression (108/1238) that a bare `DocumentSource` row count would have missed entirely, and the R2.3 fix (matching `parse_filters(args)` as well as raw `request.args.get`) removed a false negative. Recommend keeping both patterns (coverage-not-just-existence, multiple call-shape matching) as the template for any future probe additions.

## Punch list

**Cheap, safe, low-risk (no design decision needed):**
1. Add a `Bibliografický pramen`-specific note or explicit "source: N/A by design" marker if that type is intentionally exempt from R1.6 — currently indistinguishable in the data from an unaddressed gap.
2. Consider adding a lightweight regression guard (a test or a checker assertion) on R1.6's per-type coverage percentages, specifically for `Norma` (currently 96 %) — the 2026-09-16 regression to 5/1104 would have been caught immediately by such a floor check, and the `Gestor`-collapse logic that caused it (`backfill_puvodce.py`) could regress again on a future refactor.
3. Fix the stale `CLAUDE.md` line describing `wsgi.py` as importing `Web.app` (it imports `app.app` and has for several runs now) — a one-line doc correction, not a code change.

**Bigger, needs a design decision from the project owner:**
4. R1.1 — 56 documents still have `identifier IS NULL` and therefore sit outside the UNIQUE constraint's protection; decide whether these need synthetic identifiers, a `NOT NULL` policy going forward, or are accepted as a permanent, disclosed residual (own decision already made once for jurisdikce `neurčeno` residuals — same shape of tradeoff).
5. R1.3 — only 6 `IMPLEMENTS` edges exist against a national corpus of ~58 acts/regulations/decrees and ~44 EU acts; decide whether to invest in a broader transposition-linking pass (more reference extraction, possibly needing full-text access, which R4.1 partly restricts) or accept that most transpositions are not explicitly documented as relations yet.
6. R1.3/R1.5 cross-linking — `REPEALS` (0 rows) and `lifecycle_state='superseded'` (20 rows) currently don't reference each other; deciding whether a superseded `DocumentVersion` should also generate/require a `REPEALS` `document_relation` edge is a modeling decision, not a bug fix.
7. R1.7 — description coverage is 63 % and `needs_review` is 36 % of the corpus; whether to invest further pipeline effort in filling missing descriptions (vs. accepting the current disclosed-gap approach) is a scope/resourcing decision.
8. R4.2 — genuinely unverifiable from the repo; if the project wants this requirement formally satisfied, it needs an actual deployment story (hosting, who has been told about it, usage evidence) documented somewhere, not just left implicit in `PROJECT.md`'s framing.

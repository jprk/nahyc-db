# DP004 Requirements Compliance Report

**Generated:** 2026-09-18 11:10 (local) · **Git commit:** `534d8bb`

This is a point-in-time snapshot, overwritten on every audit run (not an
accumulating log). It supersedes the 2026-09-16 report — a corpus-expansion
arc (EU/Czech/HYTEP/MPO sources), a review-queue triage pass, and
process-layer citation-linking work (`load_process_layer.py`,
`app/app.py`) have landed since then.

Mechanical evidence below comes from `.venv/bin/python
src/tools/check_requirements.py` and `src/tools/check_db.py`, plus
targeted follow-up SQL for facts those scripts don't yet cover (noted
inline and in the punch list). Live DB: `h2regdocs` — 1272 `Document` rows.

## Requirement compliance table

| Requirement | Verdict | Evidence | Gap |
|---|---|---|---|
| **R1.1** Record uniqueness & dedup | **PARTIAL** | `Document.identifier` has a `UNIQUE KEY` (confirmed via `SHOW CREATE TABLE`). 1197/1272 rows have a non-NULL identifier; 75 are NULL and bypass the constraint entirely. Two of those 75 are a **confirmed live duplicate**: `Document.id` 117 and 122, both titled "Zákon o bezpečnosti a ochrane zdravia pri práci a o zmene a doplnení niektorých zákonov", same `jurisdikce='SK'`, same `url='https://www.slov-lex.sk/pravne-predpisy/SK/ZZ/2006/124/'` — a genuine same-document duplicate the schema does not prevent. | The UNIQUE constraint only protects documents that already resolved an `identifier`. No fallback uniqueness check (e.g. on `url`, or `title`+`jurisdikce`) catches NULL-identifier duplicates. Real duplicate `id` 117/122 needs manual merge; the two "Slov-Lex" placeholder-title rows (`id` 75, 96 — different URLs, same generic scraper-leftover title "Slov-Lex") are a related R1.7 metadata defect, not true duplicates. |
| **R1.2** Jurisdictional tiering | **PARTIAL** | `Document.jurisdikce_uroven` is a `GENERATED`/computed `ENUM('mezinárodní','EU','národní')` column, correctly collapsing 11 raw `jurisdikce` country codes (SK/DE/CZ/US/CA/FR/UK etc.) into the 3 required tiers. Coverage: národní=838, mezinárodní=195, EU=87, **NULL=152**. Root cause: 67 rows have `jurisdikce IS NULL` and 85 have `jurisdikce='neurčeno'` (both untiered by construction). | 152/1272 documents (12%) carry no jurisdictional tier at all, i.e. fail the "must categorize **every** document" wording literally. Mechanism/design is sound; the gap is unresolved source data (identification not yet completed for these 152 records), not a schema defect. |
| **R1.3** Transposition/dependency mapping (EU directive → national law) | **PARTIAL** | `document_relation.relation_type` ENUM includes `IMPLEMENTS`; **6 rows** exist, all genuine EU→national or EU→national-instrument links (e.g. `458/2000 Sb.` IMPLEMENTS Directive `2009/73/ES` and `(EU) 2019/692`; `56/2001 Sb.` IMPLEMENTS `2007/46/ES`). Surfaced bidirectionally on `/dokument/<slug>` (`app/app.py` `fetch_document_detail()`, ~line 394-448). Mechanism works correctly where populated. | Only **13** `Směrnice EU` documents exist in the corpus and just 6 IMPLEMENTS edges are recorded — coverage is thin relative to the corpus's likely real transposition graph (most CZ/SK national laws in energy/transport/environment domains almost certainly transpose *some* EU directive, but aren't linked). `src/tools/link_document_relations_auto.py` exists per `doc/konsolidace/Konsolidace-DB-schema.sql:109` but evidently hasn't been run to exhaustion, or the corpus lacks enough directive-text citations to match against. |
| **R1.4** Standard localization linking (ISO ↔ ČSN/STN EN ISO) | **PARTIAL — mechanism sound, coverage low** | `ADOPTS` relation type exists and is populated with **25 rows**, all correct ISO/EN/IEC ↔ national-adoption pairs (e.g. `ČSN EN ISO 11114-1` ADOPTS `ISO 11114-1`; `STN ISO 19880-1` ADOPTS `ISO 19880-1`; `DIN EN ISO 24078` ADOPTS `ISO 24078`). But a title-pattern query for `Norma`-type documents that look like a national adoption of an ISO standard (`ČSN/STN/DIN EN ISO %` or identifier `LIKE '%ISO%'`) returns **312** candidate rows — only ~25 (8%) are actually linked via `ADOPTS`. | Per the requirement's own wording ("prevent them from appearing as disconnected records"), the large majority of ISO-derived Norma records in the corpus (≈287 of ≈312) are still disconnected in practice, even though the linking mechanism and its automated loader (`link_document_relations_auto.py`) exist and work correctly for the subset already processed. This is a coverage/run-completeness gap, not a design gap. |
| **R1.5** Lifecycle & version state | **PASS** | `DocumentVersion.lifecycle_state ENUM('active','superseded','draft')` exists independent of the boolean `is_current` (`doc/konsolidace/Konsolidace-DB-schema.sql:131-133`), classified by `classify_lifecycle_state()` in `src/tools/init_db.py` (covered by tests per the schema comment). Live distribution: active=1119, draft=153, superseded=20. `is_current=TRUE` count (1272) exactly matches `Document` count — exactly one current version per document, confirmed via `check_db.py`. 20 documents have real multi-version history (base + amendment). | None found. |
| **R1.6** Authoritative source identification | **PASS** | `DocumentSource` is an independent table, 59 rows. 1169/1272 (92%) of documents carry a `source_id`. Coverage is essentially complete for legally-binding types (`Zákon` 39/39, `Směrnice EU` 13/13, `Nařízení vlády` 6/6) and high for `Norma` (1064/1104, 96%) across 40 distinct standard-setting bodies. | Weaker coverage for secondary/reference types: `Bibliografický pramen` 0/22, `Studie` 0/6, `Metodika` 0/3, `Strategický dokument` 2/10 — these are lower-priority document classes but currently have no assigned authoritative source at all. |
| **R1.7** General metadata structure | **PARTIAL** | Full column set present (`id, title, description, ..., file_path, ...`) confirmed via `SHOW COLUMNS`. `description` populated 939/1272 (74%); `language` 1250/1272; `identifier` 1197/1272; `slug` 1272/1272 (100%, stable per-doc URLs). `needs_review` flag (346 rows, 27%) transparently surfaces gaps instead of hiding them, per `doc/konsolidace/Konsolidace-DB-schema.sql:155-169` — visible in the UI, not just the DB. | `file_path` populated for only **70/1272** (5.5%) — by design, per schema comment, `fetch_fulltext.py` only ever downloads `Zákon`-type full text, so this is a structural floor, not a bug, but it means "comprehensive metadata... including file paths" is true of the schema, not of the bulk of the data. 271 of the 346 needs_review flags are "missing description/annotation" — the single largest data-quality gap. |
| **R1.8** Thematic indexing (M:N keywords) | **PASS** | `Keyword` (226 rows) ↔ `DocumentKeyword` (3231 M:N link rows) — clean many-to-many, no duplication of keyword text per document. | None found. |
| **R1.9** Automated/semi-automated maintenance | **PARTIAL** | All 6 canonical pipeline scripts present (`build_unified_db.py`, `deduplicate_db.py`, `link_document_versions.py`, `init_db.py`, `load_document_relations.py`, `load_process_layer.py`), each independently re-runnable and covered by `tests/`. The recent corpus-expansion (EU/CZ/HYTEP/MPO sources) and this cycle's process-layer citation fix (`534d8bb`) demonstrate the pipeline genuinely supports incremental updates without a full rebuild. | No scheduler, cron entry, systemd timer, or CI workflow found anywhere in the repo (checked for `*.yml`/`*.yaml`, `crontab*`, `*.service`, `*.timer` — none exist). Every run to date has been triggered by hand by a developer. "Semi-automated" (human-triggered, machine-assisted) is a fair description of the current state; "automated" (i.e. unattended, scheduled) is not yet true. |
| **R2.1** Search result uniqueness | **PASS** | `app/app.py`'s document-list query ends `GROUP BY d.id ORDER BY d.title ASC` — collapses joins against `DocumentKeyword`/keyword filters back to one row per document. Code comment references a prior regression (`ebc1f60`) that had to add this fix once already, i.e. it's a known-fragile spot that is currently correctly guarded. | None found currently; flag as regression-prone if the query is ever refactored to add another joined M:N table without re-adding `GROUP BY`. |
| **R2.2** Responsive web UI | **PASS** | `viewport` meta tag present in `base.html`; 3 `@media` breakpoints in `style.css` (992px, 768px ×2). No installed client software required — served over HTTP via Flask. | None found. |
| **R2.3** Combined full-text + structured filters | **PASS** | `app/app.py` index route parses `q`, `type_id`, `source_id`, `keyword_id` simultaneously and combines them (confirmed via grep of filter-param reads) — a single search bar plus type/source/keyword facets, applied together, not mutually exclusive. | None found. |
| **R2.4** Result metadata summary + hyperlink | **PASS** | `doc.url` referenced and rendered in `index.html`; document detail page (`fetch_document_detail`) surfaces title, description, jurisdiction, keywords, versions, and relations per result. | None found; not independently verified whether *every* result row exposes a hyperlink or only ones with a populated `url`/`file_path` — worth a follow-up grep of `index.html`'s conditional rendering if this needs tightening. |
| **R2.5** Export readiness (CSV/JSON/XML) | **PASS** | `/export/<fmt>` route in `app/app.py` supports `EXPORT_FORMATS = {"csv", "json", "xml"}`, applies the *same* filters as `index()` (shared `_rows_for_export()` helper, explicitly documented to return the full filtered set, not page-limited), and explicitly excludes `file_path`/`restricted_fulltext`/`id` from the export payload (R4.1 interaction, correctly handled). UI exposes CSV/JSON/XML buttons in `index.html`. | None found — this is a fully-realized requirement, including the R4.1 interaction most naive implementations would miss (leaking restricted fields via bulk export). |
| **R3.1** Python + Flask backend | **PASS** | `from flask import` confirmed in `app/app.py`. | None. |
| **R3.2** Relational DB, MariaDB, dynamic init scripts | **PASS (confirm, no mismatch)** | `.env` connects to `localhost:3306` via `pymysql`, live-confirmed as MariaDB. `src/tools/init_db.py` dynamically populates the schema from pipeline output, per `PROJECT.md`'s own description ("kterou dynamicky plní skript `src/tools/init_db.py`"). No stated-vs-actual mismatch found (an earlier concern about lingering `sqlite3` code in `app/app.py` was checked and is **False** — no legacy SQLite path remains). | None found. |
| **R3.3** HTML5/CSS3/Jinja2 frontend | **PASS** | 5 templates found (`base.html`, `document.html`, `process_detail.html`, `index.html`, `processes.html`); Jinja2 `{% %}`/`{{ }}` syntax confirmed in `index.html`. | None. |
| **R3.4** Vanilla JS + Phosphor Icons + Google Fonts | **PASS** | Phosphor Icons and Google Fonts references confirmed in `base.html`; zero `.js` files under `app/static` (inline vanilla JS only); no heavy JS framework references found anywhere in `app/`. | None. |
| **R4.1** Licensing/visibility rules | **PASS** | `DocumentType.restricted_fulltext` correctly flags only `Norma` (paid/copyrighted standards) as `TRUE`; all other 11 types are `FALSE`. `/fulltext/<int:doc_id>` route checks this flag and `abort(403)`s before serving (`app/app.py:654-681`). Metadata itself is never gated — always served regardless of `restricted_fulltext`. Bulk export (`/export/<fmt>`) independently excludes `file_path`/`restricted_fulltext`/`id`, closing an otherwise-easy leak path. Of the 70 documents with a populated `file_path`, 63 are unrestricted and 7 are restricted-but-still-locally-cached (correctly gated, not served). | None found — this requirement is well-implemented, with the export-path leak explicitly considered and closed. |
| **R4.2** Central public hub for stakeholders | **N/A (not mechanically checkable)** | `PROJECT.md`'s stated goal: "Host a Python web interface to a public database of hydrogen fuel related norms, laws, and other regulating documents," explicitly aimed at "investory, provozovatele infrastruktury a orgány státní správy" (investors, infrastructure operators, state administration) to reduce regulatory uncertainty. No evidence found of an actual public deployment (no CI/deploy config, no hosting reference beyond local dev — `.env`/`wsgi.py` are dev/deploy-ready but nothing indicates the app is currently live/public-facing). | Can't verify whether this is *actually serving* as a hub today, only that it's *designed* to. The requirement's specific stakeholder list ("industrial manufacturers, transport operators, and research institutions") is broader than what `PROJECT.md` names explicitly (investors, infra operators, state administration) — worth reconciling wording, though the app itself has no access restriction that would exclude any of those groups. |

## Other findings (not tied to a specific requirement)

- **`wsgi.py` import — confirmed correct, not a live bug.** `from app.app import app` resolves and runs cleanly (`.venv/bin/python -c "from app.app import app"` → `Flask 'app.app'`). `app/` has no `__init__.py` but works as an implicit Python 3 namespace package. This matches `CLAUDE.md`'s note that this was fixed 2026-09-11 (commit `ebc1f60`) — no longer an open issue, can likely be dropped from future "known findings" boilerplate.
- **No duplicate `Web.zip` archives found** at the repo root or elsewhere (checked via `find . -iname "*.zip"`, excluding `.venv`) — this issue mentioned in the audit brief as historically flagged appears to already be resolved or was specific to an earlier snapshot.
- **Confirmed live duplicate document** (`Document.id` 117 and 122, both "Zákon o bezpečnosti a ochrane zdravia pri práci...", identical URL) — see R1.1 row above; this is a concrete, fixable data bug, not a design gap.
- **Two placeholder-titled records** (`Document.id` 75 and 96, both titled literally "Slov-Lex" — a scraper artifact, not the actual law name) — distinct URLs so not true duplicates, but a real R1.7 metadata-quality defect worth its own review-queue entry (currently *not* caught by `needs_review`, since that flag targets missing descriptions/truncated titles, not placeholder titles).
- No scheduler/CI infrastructure exists anywhere in the repo (see R1.9) — every pipeline run and every "automated" metadata-fetch pass to date has been developer-triggered.

## Punch list

### Cheap / safe fixes (mechanical, low-risk, could be done next)
1. Merge or deduplicate `Document.id` 117 and 122 (identical SK law, same URL) — a concrete, isolated R1.1 fix.
2. Fix or flag the two "Slov-Lex" placeholder titles (`id` 75, 96) — either re-derive the real title from the cited URL or add them to the `needs_review` queue under a new reason category ("placeholder/generic title").
3. Extend `src/tools/check_requirements.py`'s R1.1 section to also report duplicate `(title, jurisdikce)` pairs among NULL-identifier rows (not just the raw NULL count) — this run found the id-117/122 duplicate via an ad hoc query the checker doesn't yet run; per the audit methodology this should become a standing check rather than a one-off.
4. Extend `check_requirements.py`'s R1.4 section to report the ISO-title-pattern-candidate-vs-actually-`ADOPTS`-linked ratio (312 candidates vs 25 linked, found via ad hoc query here) — currently the checker only reports raw `ADOPTS` row counts, which looks fine in isolation but hides the coverage gap.
5. Run (or re-run) `src/tools/link_document_relations_auto.py` over the full current corpus to close some of the R1.3/R1.4 coverage gap — cheap if the tool already exists and works (per its use in the 25 existing `ADOPTS` rows), just hasn't been run to exhaustion against the expanded corpus.

### Bigger / needs project-owner judgement
6. **R1.2 tiering gap (152 untiered documents):** resolving `jurisdikce IS NULL`/`'neurčeno'` requires actual source research per document (identifying country/jurisdiction), not a schema change — an editorial/data-entry task, not code.
7. **R1.3/R1.4 coverage completeness:** even after re-running the automatic linker (#5), some fraction of the corpus will likely need manual relation curation (`data/document_relations.json`) — decide how much manual curation effort is worth investing given diminishing returns, and whether "disconnected records" is acceptable for older/deprecated documents.
8. **R1.9 "automated" vs "semi-automated":** decide whether a real scheduler (cron/systemd timer/CI job) is in scope for this project phase, or whether the requirement's wording should be revised to match the manual-trigger reality — this is a genuine mismatch between `doc/REQUIREMENTS.md`'s phrasing ("automated or semi-automated") and the implementation (semi-automated only, no unattended scheduling), worth resolving explicitly rather than leaving ambiguous.
9. **R4.2 public-hub status:** confirm (outside this repo, likely with the project owner) whether/where the app is actually deployed and publicly reachable — this audit found no deploy/hosting evidence either way, and the requirement is inherently about real-world usage, not code.
10. **R1.6 coverage for secondary document types** (`Bibliografický pramen`, `Studie`, `Metodika`, `Strategický dokument` — all near-zero `source_id` coverage): decide whether these types warrant the same authoritative-source rigor as primary legal documents, or are intentionally out of scope for R1.6.

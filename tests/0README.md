# Adresář /tests

Tento adresář obsahuje automatizované testy (unittest, stdlib — bez závislosti na pytestu). Spuštění celé sady: `.venv/bin/python -m unittest discover -s tests -p "test_*.py"` z kořene repozitáře.

## Obsah
* `test_search.py` - Testovací skript ověřující správnost fungování vyhledávání a deduplikace záznamů podle názvu ve Flask aplikaci (`app/app.py`), např. že zákon 458/2000 Sb. se po deduplikaci zobrazí v katalogu právě jednou. Vyžaduje běžící MariaDB (`h2regdocs`) a `.env` — jde o integrační test proti reálné databázi, ne izolovaný unit test. `FulltextRouteTestCase` (R1.7/R4.1, 2026-09-11) ověřuje totéž pro `/fulltext/<id>`: restriktovaný typ (`"Norma"`) s `file_path` → `403`, otevřený typ s `file_path` → `200`, dokument bez `file_path` → `404`, neexistující id → `404` — vyhledá si reálné příklady z živé DB přes `setUp`, přeskočí (`skipTest`), pokud korpus zrovna žádný nemá. `ExportRouteTestCase` (R2.5/R4.1, 2026-09-11) ověřuje `/export/<fmt>` proti živé DB: každý formát (`csv`/`json`/`xml`) vrací `200` se správným `content_type`, neplatný formát `400`, filtr `type_id` reálně sníží počet exportovaných řádků, a — klíčová regrese — žádný exportovaný řádek v žádném formátu neobsahuje `file_path`/`restricted_fulltext`/`id`.
* `test_build_unified_db.py` - Unit testy pro `extract_znacka_from_title()` (`src/tools/build_unified_db.py`) — pokrývají všechny vzory nalezené v reálných datech Haltuf/Sinay (viz `doc/PLAN.md` Step 1, follow-upy #3/#4/#6) i obě zde opravené regrese — a pro `resolve_prokop_jurisdikce()` (Krok 1 follow-up #14): holá mezinárodní ISO/IEC značka je vždy `"mezinárodní"`, ne `"CZ"`, i když přišla ze zdroje Prokop; (doc/PLAN.md §9, 2026-09-13) i holé `"EN <číslo>"` bez ISO/IEC je `"EU"`. `record_url`/`apply_authoritative_metadata` (doc/PLAN.md §8, 2026-09-11): fetched záznam z cache připojí `nazev_autoritativni`/`popis_autoritativni`/`zdroj_autoritativni_url` beze změny `nazev_cz`/`anotace_poznamka`; `"csn:<znacka>"` klíč se použije, když záznam nemá URL; `failed` status i chybějící cache hit nic nepřipojí; (§9) `zdroj_autoritativni_url` z cache vyhrává nad `record_url()`; `jurisdikce_autoritativni` se aplikuje přímo do `jurisdikce` se zachováním originálu v `jurisdikce_puvodni`. `synthesize_csn_adoption_records` (§9): přidá nový záznam jen když daná značka ještě v `unified_db` neexistuje, idempotentně i napříč dvěma cache záznamy mířícími na stejnou syntetizovanou značku. `classify_law_document_typ` (doc/PLAN.md §11/§14, 2026-09-14/15, uživatelský nález): každý testovaný případ je reálný titulek z korpusu — český/slovenský zákon/zákoník (vč. složeninového tvaru "Energetický zákon" nezačínajícího slovem "Zákon"), vyhláška (vč. skloněných tvarů a MV SR prefixu), nařízení vlády, EU nařízení/směrnice/rozhodnutí v ČEŠTINĚ/SLOVENŠTINĚ i ANGLIČTINĚ (vč. varianty bez `"(EU)"` závorky, jen s "komisie"/"rady"/"European Central Bank"), regresní test na to, že vlastní typ aktu vyhrává nad pozdější zmínkou JINÉHO citovaného/rušeného aktu jiného typu (nejdřívější pozice klíčového slova, ne pevné pořadí), holé normové značky → `"Norma"`, a genuinely nerozpoznané případy (politická strategie, Sdělení Komise, EHK OSN, ADR, RID) vracející `""`. `split_sinay_zakony_row` (doc/PLAN.md §15, 2026-09-15): primární záznam (CZ, nebo SK fallback), SK-protějšek jen pro skutečně odlišný obsah (ne pro identický text ani pro holou citaci EU aktu v poli SK), EU originál vždy když je pole vyplněné, `NATIONAL_EQUIVALENT` vztah jen pro skutečný CZ+SK pár, `nazev_eu`/`odkaz_eu` zůstávají na primárním záznamu, a regresní test na sbalení EU NAŘÍZENÍ (přímý účinek — žádná samostatná národní implementace) do jednoho EU-úrovňového záznamu místo spurious 2-3 duplicitů. Čisté, bez síťových volání.
* `test_deduplicate_db.py` - Unit testy pro klíčovou logiku `src/tools/deduplicate_db.py`: normalizaci značky (vč. sjednocení variant pomlčky, odstranění všech tří stylů koncové edice-datové přípony — "/ - RRRR.MM", holé "kolonka:RRRR" i SAE styl "kolonka:RRRR-MM" — s ošetřením proti záměně s konvencí "část:2místý rok" — sjednocení oddělovače u dvou konkrétních řad (CSA/ANSI, IGEM/TD/1) a sjednocení organizačního přejmenování EIGA/IGC Doc, s regresním testem, že různá vydání téhož EIGA/IGC kódu zůstanou nesloučená — ale NE značky novely a NE obecně každé lomítko, viz `doc/PLAN.md` Krok 1 follow-up #10/#11/#12/#13/#14), `validate_merge`, `is_pure_znacka_cluster`, `programmatic_merge`, `match_type_for_group`, `build_clusters` (na syntetických embeddingech, bez OpenAI) — včetně veta při konfliktní `jurisdikce` a regresního testu na reálně nalezenou chybu (sloučení slovenské STN do české ČSN), a `test_bridging_unknown_jurisdiction_record_never_joins_two_known_conflicting_islands` (2026-09-11: druhá reálně nalezená chyba — párová kontrola konfliktu nechytí přemostění PŘES záznam s neznámou jurisdikcí; ověřuje, že žádný výsledný ostrov nikdy nesmí obsahovat víc než jednu ZNÁMOU jurisdikci, bez ohledu na pořadí zpracování), `find_iso_csn_ambiguous_groups`/ověření proti registru ČSN online (`resolve_iso_csn_ambiguity`, s mockovaným vyhledáváním) a `deduplicate_cluster_with_llm` (s mockovaným OpenAI klientem — žádné reálné API volání). (doc/PLAN.md §9, 2026-09-13) `programmatic_merge` navíc: autoritativní pole (`nazev_autoritativni`/`popis_autoritativni`/`zdroj_autoritativni_url`/`jurisdikce_autoritativni`/`jurisdikce_puvodni`) se doplní z KTERÉHOKOLI člena shluku, ne jen z toho s nejdelším `nazev_cz` (skutečná chyba nalezena živě — viz `src/tools/0README.md`).
* `test_parse_sinay_norms.py` - Unit testy pro `src/tools/parse_sinay_norms.py`: `classify_jurisdikce()` (STN→SK, německé/mezinárodní/EU/US orgány, bezpečné selhání na "neurčeno" u nerozpoznaného zdroje, regresní test na to, že STN i DIN vyhrávají nad citovaným mezinárodním výborem v poli Kategorie — follow-up #12 — a že holá mezinárodní ISO/IEC značka je vždy "mezinárodní" bez ohledu na okolní text, i když ten zmiňuje STN — follow-up #14), `_pdf_row_to_record()` (Kategorie/Platnost/Jurisdikce/Link mapování, vč. opravy mezery v URL po zalomení, i rozpoznání zástupného textu "žádné označení"), `is_placeholder_designation()`, `merge_designation_continuations()` (odřádkovaná koncovka značky — datum i krátký číselný/interpunkční zbytek, vč. regresního testu 2026-09-11 na variantu, kdy pomlčka pokračování wrapuje NA další řádek — `"...62443-1-5/"` + `"- 2024.09"` — ne jen zůstává na první, `"...15502-3-1/ -"` + `"2024.08"`) a `parse_xlsx()` end-to-end nad malým in-memory sešitem (openpyxl) ověřující, že řádek se zástupným označením ale reálným názvem zůstává zachován, ne zahozen, a že holá mezinárodní ISO/IEC značka ve sloupci STN designace nepřebije jurisdikci na "SK" (follow-up #14). `ParsePdfPageTestCase` (2026-09-11, end-to-end přes fake `pdfplumber.Page`, reprodukuje reálné souřadnice slov, které se rozbily): řádek se sedícím na designaci technické komise (`"CEN/TC 326 Natural Gas Vehicles"` + přetečený titulek `"- Fuelling and Operation"`) dostane prázdný titulek (a je tedy zahozen existujícím filtrem, jak modul odjakživa dokumentoval), a leading-dash datová pokračovací řádka neukradne skutečné pokračování titulku dalšímu řádku.
* `test_fetch_fulltext.py` - Unit testy pro `src/tools/fetch_fulltext.py`: `is_fetchable_source` (doc/PLAN.md §15, 2026-09-15: source-agnostic, klíčováno na `typ_dokumentu`, ne `zdroj_dat` — `"Norma"` vždy vyloučeno, ostatní typy i prázdný typ projdou), `is_norm_designation` (R4.1, 2026-09-11: tvar značky samotné jako druhá, na zdroji nezávislá kontrola — reálné korpusové případy jako `"ČSN EN 17127"`, které by zdrojový filtr sám nezachytil, protože přišly ze zákonového zdroje `Haltuf_Dokumenty`), výběr cílů k stažení (vč. že norma-tvarovaná značka je vyloučena i z jinak stahovatelného zdroje), `first_url()` (jen první URL ze dvou odřádkováním spojených), odvození přípony souboru, idempotence manifestu, a `fetch_one` s mockovanou `requests` session (žádné reálné síťové volání).
* `test_screen_eurlex.py` / `test_screen_esbirka.py` / `test_screen_slovlex.py` - Unit testy pro trojici screeningových skriptů: čistá diffovací logika (`find_new_candidates`/`find_new_uris`, odvození znacky z CELEX kódu) a `check_url`/HTTP vrstva s mockovanou `requests` session — bez reálných síťových volání.
* `test_init_db.py` - Unit testy pro `src/tools/init_db.py` (Krok 2): `resolve_identifier` (kolize, přehnaná délka nad `VARCHAR(100)`, prázdná znacka), `resolve_document_type` (fallback `"Nezařazeno"` pro číselné/prázdné hodnoty), `normalize_jurisdikce`, `build_gestor_jurisdiction_map`/`resolve_source_jurisdiction` (pořadí zpracování záznamů nesmí ovlivnit výsledek), `classify_lifecycle_state` (R1.5, 2026-09-11: nenaplněná/neaktuální verze je vždy `"superseded"` bez ohledu na vlastní text; aktuální verze s reálným draft/work-item markerem — `"Entwurf"`/`"Návrh"`, `"Arbeitsdokument"`/`"pracovný dokument"`, `"PWI"` — je `"draft"`; jinak `"active"`), `resolve_document_versions` (Krok 1 follow-up #16: záznam bez seznamu `versions` dostane historické chování — jeden řádek, `version=1`, `is_current=TRUE`; se seznamem dostane jeden řádek na položku, v pořadí, s vlastním `edition_label`/`effective_date`/`is_current`; od R1.5 i každý řádek dostane `lifecycle_state`, vč. regresního testu, že neverzovaný záznam použije svůj vlastní `platnost` pro klasifikaci, aniž by ho zapsal do `effective_date` sloupce té verze), `is_restricted_document_type` (R4.1: `"Norma"` restriktováno, zákony a fallback ne), `resolve_file_path` (R1.7: nalezne shodu podle KAŽDÉHO přispívajícího zdroje ve sloučeném, čárkou spojeném `zdroj_dat`; `"failed"` záznam v manifestu se nepočítá; prázdná značka i chybějící záznam v manifestu → `None`), `is_garbled_znacka`/`is_fragment_title`/`detect_data_quality_issues` (2026-09-11, uživatelský nález přes export: holá edice-datová přípona nebo odkaz na technickou komisi je "garbled" značka; useknutý fragment titulku je detekován i pro malým písmenem začínající text, ale NE pro legitimní česká/slovenská právní označení jako "zákon č. ..."/"vyhláška č. ..."; chybějící popis je vlastní, nezávislý důvod — kombinace všech tří dá seznam důvodů, prázdný seznam pro čistý záznam), `resolve_title`/`resolve_description` (doc/PLAN.md §8, 2026-09-11: `nazev_autoritativni`/`popis_autoritativni` vyhrává, pokud je vyplněné, jinak spadne na dosavadní spreadsheetová pole; prázdné autoritativní pole nevyhrává), a regresní test, že autoritativní titulek/popis "opraví" `detect_data_quality_issues()`'s příznak i beze změny surového `nazev_cz`/`anotace_poznamka`. `resolve_url` (doc/PLAN.md §9, 2026-09-13, stejný vzor jako `resolve_title`/`resolve_description`): `zdroj_autoritativni_url` vyhrává nad `odkaz_hlavni`/`odkaz_eu`/`odkaz_sk`, prázdné autoritativní URL nevyhrává. Čisté, bez připojení k databázi — `tests/test_search.py` zůstává jediným testem vyžadujícím reálnou, právě načtenou `h2regdocs`.
* `test_sites_eurlex.py` / `test_sites_zakonyprolidi.py` / `test_sites_slovlex.py` / `test_sites_esbirka.py` - Unit testy pro `src/sites/*.py` (doc/PLAN.md §8, 2026-09-11): `eurlex` — CELEX extrakce jen z `?uri=CELEX:...`/URL-encoded tvaru (ELI-styl a holé `OJ:` reference záměrně `None`, bez SPARQL dotazu vůbec — `session.get.assert_not_called()`), preference českého názvu před anglickým; `zakonyprolidi` — `og:title`/`og:description` extrakce z reálné fixtury, fallback na `<title>`, cached_path před živým dotazem; `slovlex` — JSON-LD `Legislation` blok jako primární zdroj, fallback na `<title>` se svlečenou příponou, i při poškozeném JSON-LD; `esbirka` — sestavení ELI/referenční URL ze znacky, `verify()` nikdy nedotazuje síť pro ne-českou značku, a nepotvrzená/chybějící citace vrací `None`, nikdy neuhaduje. Mockovaná `requests`/SPARQL vrstva, žádná reálná síťová volání.
* `test_check_csn_validity.py` - Unit testy pro `src/tools/check_csn_validity.py` (doc/PLAN.md §9, 2026-09-13, NOVÝ soubor): `strip_catalog_suffix`/`split_edition` (formátovací normalizace), `find_best_match` (každý konkrétní případ nalezený živě proti agentura-cas.cz — koncová `(336000)`, `ED.2` vs `ed. 2`, chybějící edice na jedné straně, preferuj-platnou-jinak-nejnovější-skutečnou-edici, `Změna ke stažení`/`Oprava ke stažení` řádky nikdy nevyhrají, zcela odlišné označení → `None`), `fetch_detail` (parsuje `<span id="...">` pole + `GridView2` tabulku z mockovaného HTML, síťová chyba → `None`).
* `test_fetch_authoritative_metadata.py` - Unit testy pro `src/tools/fetch_authoritative_metadata.py` (doc/PLAN.md §8, 2026-09-11): `is_law_record`/`is_csn_norm_record` (doc/PLAN.md §15, 2026-09-15: přepsáno na `typ_dokumentu`-based kontrakt, source-agnostic — `is_law_record` je `!= "Norma"`, vč. prázdné hodnoty; `is_csn_norm_record` vyžaduje `== "Norma"` a ČSN-prefix nebo holou EN/ISO/IEC značku), `csn_core` (strhává `ČSN`/`CSN` prefix pro `check_csn_validity.py`'s vyhledávací pole), `record_url` (stejná priorita jako `init_db.py`), `dispatch_site_module` (namapuje doménu na správný `src/sites/*` modul), a `fetch_csn_metadata` (vrátí titulek jen pro PŘESNOU shodu označení, ne první/libovolný výsledek vyhledávání). **Rozšířeno per doc/PLAN.md §9 (2026-09-13)**: `is_csn_norm_record`/`is_bare_international_znacka`/`bare_jurisdikce_tier` pro rozšířený zdroj+holé-značky rozsah; `fetch_csn_metadata` pro holou značku (anglický název z detail stránky), "přebytečné EN" fallback (na holé I již-ČSN-prefixované značce).
* `test_app_export.py` - Unit testy pro `app/app.py` (R2.5, 2026-09-11): `build_document_query` (žádné filtry → žádný extra `AND`/žádný `LIMIT`; každý filtr jednotlivě i kombinovaně; klíčová regrese — `limit=None` nikdy nepřidá `LIMIT` klauzuli vůbec, na rozdíl od `limit=100` u `index()`) a `_rows_for_export` (výstupní klíče jsou přesně `EXPORT_FIELDS`, nikdy víc — druhá, na DB nezávislá pojistka, že `file_path`/`restricted_fulltext`/`id` nikdy neuniknou; `None`/chybějící štítky → prázdný řetězec). **Rozšířeno (2026-09-13)**: `get_git_version` (reálné repo vrátí neprázdný řetězec; `git` chybí/selže → `None`, nikdy nevyhodí výjimku) a `get_db_last_updated` (`MAX(updated_at)` z mockovaného kurzoru; prázdná tabulka → `None`) — footer metadata, doc/PLAN.md §10. **Rozšířeno (doc/PLAN.md §16, 2026-09-16)**: stránkování — `build_document_query` s `offset` (první strana `OFFSET` klauzuli nepřidává; `offset` bez `limit` se ignoruje, takže export nikdy nestránkuje), `build_count_query` (klíčová regrese: MUSÍ počítat `COUNT(DISTINCT d.id)`, protože join na `DocumentKeyword` násobí řádky počtem klíčových slov — obyčejný `COUNT(*)` by nafoukl součet a stránkoval do prázdných stran; a musí aplikovat TOTOŽNÉ filtry jako seznam), `parse_page` (nesmysl v URL — `abc`/`-5`/`999` — spadne na platnou stranu, nikdy nevyhodí výjimku) a `build_page_range` (první/aktuální/poslední vždy přítomny, `None` značí vypuštěnou mezeru, čísla vzestupná a unikátní). Čisté, bez připojení k databázi.
* `test_link_document_versions.py` - Unit testy pro `src/tools/link_document_versions.py` (Krok 1 follow-up #16/#18/#19): `find_orphan_amendments` (novela bez nalezeného základu je osiřelá; novela, jejíž základ existuje — ať už přes shodnou novelizační příponu, nebo přes EN/EN-IEC pár — osiřelá není; holý základ bez novely nikdy osiřelý), `version_group_key` (seskupení základ+novela přes všechny 4 reálně nalezené tvary novelizační přípony — `+A1` před lomítkem, `/A1`/`/AC` PŘED datovou příponou, `/A1/` mezi dvěma lomítky — bez kolize nesouvisejících značek; a volitelné `fold_en_iec_renumbering=True` sbalující "EN IEC" → "EN" jen na požádání, bez kolize různých čísel dílů), `amendment_level` (0 pro nenovelizovaný základ, číslo novely jinak, `/AC` jako úroveň 1), `_has_en_iec_renumbering`, `version_sort_key` (IEC-přečíslovaný tvar řadí se jako novější než holý "EN ...", novelizační přípona jako ještě silnější signál), `build_version_groups` (vyžaduje shodnou jurisdikci a buď aspoň jednoho člena se skutečnou novelizační příponou, NEBO skutečný předěl EN/EN-IEC uvnitř skupiny — nikdy nesmete dohromady náhodnou shodu jádra značky bez jednoho z těchto dvou strukturálních důkazů; regresní test, že dva už-přečíslované "EN IEC" záznamy samy o sobě skupinu netvoří), `merge_version_group` (pole aktuální/nejnovější verze vyhrávají — vč. IEC-přečíslovaného vydání coby aktuálního, klíčová slova a zdroje se sjednocují, seznam `versions` je seřazený a přesně jeden člen má `is_current=True`) a `link_document_versions` end-to-end (nedotčené záznamy beze změny projdou).
* `test_load_document_relations.py` - Unit testy pro `src/tools/load_document_relations.py` (Krok 1 follow-up #16): `resolve_relations` — spárování obou identifikátorů, nenapárovaný `from`/`to` identifikátor i neznámý `relation_type` jdou do fronty k lidské kontrole (nikdy se nehádá), vč. `NATIONAL_EQUIVALENT` jako platného typu (doc/PLAN.md §15). `load_relation_sources` (§15): sloučí všechny tři zdrojové soubory, chybějící `document_relations_split.json` se tiše přeskočí. `dedupe_resolved_relations` (§15, skutečná chyba nalezena živě): přesně duplicitní `(from, to, relation_type)` trojice se zahodí, různý `relation_type` pro stejný pár se zachová. Čisté, bez připojení k databázi.
* `test_parse_v02_processes.py` - Unit testy pro `src/tools/parse_v02_processes.py` (Krok 3a): rozpoznání nadpisu uzlu, `extract_list_items` (upřednostnění stylu `List Paragraph`, fallback na `Normal` odstavce bez úvodní věty končící dvojtečkou), `split_branch_heading` (vč. průřezové větve a variant pomlčky), detekce Krok-větvení a průřezových nadpisů, `split_problem_text`, `parse_subject_cell`, `parse_bibliography_line`, `extract_citations` (české zákony, nařízení vlády, nařízení EU v závorce i bez ní).
* `test_load_process_layer.py` - Unit testy pro `src/tools/load_process_layer.py`: `normalize_citation_core`/`document_identifier_digit_core`/`build_document_lookup`/`match_citation`, vč. regresního testu, že se slovenská STN a česká ČSN adopce stejného čísla normy nikdy nesplynou v citační shodě.
* `test_link_document_relations_auto.py` - Unit testy pro `src/tools/link_document_relations_auto.py` (R1.3/R1.4, 2026-09-11): `international_core` (strhává národní prefix i mezivrstvý "EN " před ISO/IEC, ale ponechá holé "EN 1717" nedotčené, bez kolize různých čísel dílů), `jurisdikce_tier`, `find_localization_pairs` (skupina se propojí jen s aspoň jedním mezinárodním/EU rodičem A aspoň jedním národním dítětem; víc mezinárodních vydání = víc hran; dvě mezinárodní nebo dvě národní samy o sobě nelinkují), `digit_core`, `is_eu_act_znacka` a `find_eu_transposition_pairs` — vč. regresního testu, že EU akt citující svého rodiče (`"(EU) 2023/1184"` → `"(EU) 2018/2001"`) NEVYTVOŘÍ hranu (rozlišeno podle tvaru VLASTNÍ značky citujícího, ne podle `jurisdikce`, protože ta je v korpusu prázdná jak u národních zákonů, tak u EU aktů), že národní zákon s prázdnou `jurisdikce` (reálný stav zdrojů Sinay_Zakony/Haltuf_Dokumenty) přesto linkuje správně, že záznam bez použitelné vlastní značky nikdy neprodukuje hranu ani kandidáta, a že sebe-citace na začátku textu neschová pozdější skutečně odlišnou citaci ve stejném poli (`finditer`, ne jen první shoda). `find_localization_pairs` (doc/PLAN.md §15, 2026-09-15, skutečná chyba nalezena živě): nikdy nepropojí záznam sám se sebou, i když sdílí značku se sebou samým pod jinou jurisdikcí.
* `test_standards_body.py` - Unit testy pro `src/tools/standards_body.py` (doc/PLAN.md §16, 2026-09-16): označení normy identifikuje svého vydavatele — národní institut i pro adopci evropské/mezinárodní normy (`STN EN ISO ...` je vydaná slovenským institutem), všechny návrhové stupně ISO (`ISO/AWI`/`ISO/DIS`/...) patří ISO a nesmí být zastíněny holým tokenem `ISO`, celá DVGW rodina (`G`/`GW`/`C`/`ZP`/`Gas-Information`/`G269`) míří na jeden orgán, `EIGA` a jeho dřívější název `IGC` se shodují, edice-datová přípona se strhne před porovnáním, a — klíčové — nerozpoznané označení vrací `None`, nikdy neuhádne.
* `test_slug.py` - Unit testy pro `src/tools/slug.py` (doc/PLAN.md §16, 2026-09-16): `slugify` (diakritika, interpunkce, žádná úvodní/koncová pomlčka), `base_slug` (preferuje označení; edice-datová přípona se do slugu nedostane; regresní testy na obě chyby nalezené 2026-09-16 — číslo označení po pomlčce (`ZP-5101`) se NESMÍ strhnout, zatímco koncový ROK u US/AU označení (`ASME B31.12-2019`) ano, a en-dash varianta oddělovače se strhává stejně jako ASCII; bez skutečného označení hash z titulku) a `assign_slugs` — **klíčový invariant: přiřazení je nezávislé na pořadí vstupu**, takže dvě přesestavení dají identické slugy; kolizní přípony se udělují podle obsahu, ne podle klíče.
* `test_language.py` - Unit testy pro `src/tools/language.py` (doc/PLAN.md §16, 2026-09-16 — modul vznikl 2026-09-15 bez testů): `normalize_raw_language` (`"čeština"`/`"angličtina"`/`"CZ"` → `CS`/`EN`, `"CZ"` je kód země, ne jazyka; neznámá hodnota → `None`, aby se spustila detekce) a `detect_language` — **klíčová regrese, kvůli které modul existuje: slovenský titulek s NĚMECKÝM popisem musí vyjít `SK`** (detekce nad spojeným `title + description` označila 217 ze 424 `jurisdikce="SK"` záznamů jako `DE`, protože dlouhý popis přebil krátký titulek); popis se použije jen když je titulek prázdný; bez čehokoli použitelného vrací `EN` s `confident=False`, nikdy netvrdí jistotu; výsledek je vždy jeden ze čtyř kódů a je deterministický.
* `test_norm_title.py` - Unit testy pro `src/tools/norm_title.py` (doc/PLAN.md §16, 2026-09-16 — modul vznikl 2026-09-15 bez testů): `designation_core` (strhne Sinayovu edice-datovou příponu vč. en-dash varianty i holý koncový rok u US/AU označení, ale NEsmí strhnout číslo označení, které jen náhodou stojí za pomlčkou — `ZP-5101`, `SAND2012-7321`), `is_real_designation` (kopie titulku bez číslice není označení) a `format_norm_title` (označení dopředu; koncová závorková varianta `(IEC/TR 62351-13:2016)` se přesune dopředu, ne zduplikuje; idempotentní; bez skutečného označení titulek beze změny; titulek nikdy nezůstane prázdný).
* `test_puvodce.py` - Unit testy pro `src/tools/puvodce.py` (doc/PLAN.md §16, 2026-09-16 — modul vznikl 2026-09-15 bez testů): `gestor_list`/`is_clean_institution_name` (víceřádkový LLM "blob" se seznamem DG a volným komentářem není jméno instituce), `resolve_puvodce` (první ČISTÁ položka vyhrává, blob se přeskočí; holá zkratka se rozvine, aby `MPO` a `Ministerstvo průmyslu a obchodu` nebyly dva různé `DocumentSource` řádky; `MZ` = zdravotnictví, ověřeno z obsahu záznamu, ne uhodnuto ze zkratky) a `resolve_gestor` — všechny tři větve: národní akt → ministerstvo, EU akt → orgán EU z cache (a když v cache není, je označen k revizi, NE degradován na české ministerstvo), `Norma` → vydavatel z vlastního označení.

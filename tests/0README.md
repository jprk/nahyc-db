# Adresář /tests

Tento adresář obsahuje automatizované testy (unittest, stdlib — bez závislosti na pytestu). Spuštění celé sady: `.venv/bin/python -m unittest discover -s tests -p "test_*.py"` z kořene repozitáře.

## Obsah
* `test_search.py` - Testovací skript ověřující správnost fungování vyhledávání a deduplikace záznamů podle názvu ve Flask aplikaci (`app/app.py`), např. že zákon 458/2000 Sb. se po deduplikaci zobrazí v katalogu právě jednou. Vyžaduje běžící MariaDB (`h2regdocs`) a `.env` — jde o integrační test proti reálné databázi, ne izolovaný unit test.
* `test_build_unified_db.py` - Unit testy pro `extract_znacka_from_title()` (`src/tools/build_unified_db.py`) — pokrývají všechny vzory nalezené v reálných datech Haltuf/Sinay (viz `doc/PLAN.md` Step 1, follow-upy #3/#4/#6) i obě zde opravené regrese — a pro `resolve_prokop_jurisdikce()` (Krok 1 follow-up #14): holá mezinárodní ISO/IEC značka je vždy `"mezinárodní"`, ne `"CZ"`, i když přišla ze zdroje Prokop. Čisté, bez síťových volání.
* `test_deduplicate_db.py` - Unit testy pro klíčovou logiku `src/tools/deduplicate_db.py`: normalizaci značky (vč. sjednocení variant pomlčky, odstranění všech tří stylů koncové edice-datové přípony — "/ - RRRR.MM", holé "kolonka:RRRR" i SAE styl "kolonka:RRRR-MM" — s ošetřením proti záměně s konvencí "část:2místý rok" — sjednocení oddělovače u dvou konkrétních řad (CSA/ANSI, IGEM/TD/1) a sjednocení organizačního přejmenování EIGA/IGC Doc, s regresním testem, že různá vydání téhož EIGA/IGC kódu zůstanou nesloučená — ale NE značky novely a NE obecně každé lomítko, viz `doc/PLAN.md` Krok 1 follow-up #10/#11/#12/#13/#14), `validate_merge`, `is_pure_znacka_cluster`, `programmatic_merge`, `match_type_for_group`, `build_clusters` (na syntetických embeddingech, bez OpenAI) — včetně veta při konfliktní `jurisdikce` a regresního testu na reálně nalezenou chybu (sloučení slovenské STN do české ČSN), `find_iso_csn_ambiguous_groups`/ověření proti registru ČSN online (`resolve_iso_csn_ambiguity`, s mockovaným vyhledáváním) a `deduplicate_cluster_with_llm` (s mockovaným OpenAI klientem — žádné reálné API volání).
* `test_parse_sinay_norms.py` - Unit testy pro `src/tools/parse_sinay_norms.py`: `classify_jurisdikce()` (STN→SK, německé/mezinárodní/EU/US orgány, bezpečné selhání na "neurčeno" u nerozpoznaného zdroje, regresní test na to, že STN i DIN vyhrávají nad citovaným mezinárodním výborem v poli Kategorie — follow-up #12 — a že holá mezinárodní ISO/IEC značka je vždy "mezinárodní" bez ohledu na okolní text, i když ten zmiňuje STN — follow-up #14), `_pdf_row_to_record()` (Kategorie/Platnost/Jurisdikce/Link mapování, vč. opravy mezery v URL po zalomení, i rozpoznání zástupného textu "žádné označení"), `is_placeholder_designation()`, `merge_designation_continuations()` (odřádkovaná koncovka značky — datum i krátký číselný/interpunkční zbytek) a `parse_xlsx()` end-to-end nad malým in-memory sešitem (openpyxl) ověřující, že řádek se zástupným označením ale reálným názvem zůstává zachován, ne zahozen, a že holá mezinárodní ISO/IEC značka ve sloupci STN designace nepřebije jurisdikci na "SK" (follow-up #14).
* `test_fetch_fulltext.py` - Unit testy pro `src/tools/fetch_fulltext.py`: klasifikace zdroje (normy vždy přeskočeny), výběr cílů k stažení, `first_url()` (jen první URL ze dvou odřádkováním spojených), odvození přípony souboru, idempotence manifestu, a `fetch_one` s mockovanou `requests` session (žádné reálné síťové volání).
* `test_screen_eurlex.py` / `test_screen_esbirka.py` / `test_screen_slovlex.py` - Unit testy pro trojici screeningových skriptů: čistá diffovací logika (`find_new_candidates`/`find_new_uris`, odvození znacky z CELEX kódu) a `check_url`/HTTP vrstva s mockovanou `requests` session — bez reálných síťových volání.
* `test_init_db.py` - Unit testy pro `src/tools/init_db.py` (Krok 2): `resolve_identifier` (kolize, přehnaná délka nad `VARCHAR(100)`, prázdná znacka), `resolve_document_type` (fallback `"Nezařazeno"` pro číselné/prázdné hodnoty), `normalize_jurisdikce`, `build_gestor_jurisdiction_map`/`resolve_source_jurisdiction` (pořadí zpracování záznamů nesmí ovlivnit výsledek), `resolve_document_versions` (Krok 1 follow-up #16: záznam bez seznamu `versions` dostane historické chování — jeden řádek, `version=1`, `is_current=TRUE`; se seznamem dostane jeden řádek na položku, v pořadí, s vlastním `edition_label`/`effective_date`/`is_current`). Čisté, bez připojení k databázi — `tests/test_search.py` zůstává jediným testem vyžadujícím reálnou, právě načtenou `h2regdocs`.
* `test_link_document_versions.py` - Unit testy pro `src/tools/link_document_versions.py` (Krok 1 follow-up #16/#18/#19): `find_orphan_amendments` (novela bez nalezeného základu je osiřelá; novela, jejíž základ existuje — ať už přes shodnou novelizační příponu, nebo přes EN/EN-IEC pár — osiřelá není; holý základ bez novely nikdy osiřelý), `version_group_key` (seskupení základ+novela přes všechny 4 reálně nalezené tvary novelizační přípony — `+A1` před lomítkem, `/A1`/`/AC` PŘED datovou příponou, `/A1/` mezi dvěma lomítky — bez kolize nesouvisejících značek; a volitelné `fold_en_iec_renumbering=True` sbalující "EN IEC" → "EN" jen na požádání, bez kolize různých čísel dílů), `amendment_level` (0 pro nenovelizovaný základ, číslo novely jinak, `/AC` jako úroveň 1), `_has_en_iec_renumbering`, `version_sort_key` (IEC-přečíslovaný tvar řadí se jako novější než holý "EN ...", novelizační přípona jako ještě silnější signál), `build_version_groups` (vyžaduje shodnou jurisdikci a buď aspoň jednoho člena se skutečnou novelizační příponou, NEBO skutečný předěl EN/EN-IEC uvnitř skupiny — nikdy nesmete dohromady náhodnou shodu jádra značky bez jednoho z těchto dvou strukturálních důkazů; regresní test, že dva už-přečíslované "EN IEC" záznamy samy o sobě skupinu netvoří), `merge_version_group` (pole aktuální/nejnovější verze vyhrávají — vč. IEC-přečíslovaného vydání coby aktuálního, klíčová slova a zdroje se sjednocují, seznam `versions` je seřazený a přesně jeden člen má `is_current=True`) a `link_document_versions` end-to-end (nedotčené záznamy beze změny projdou).
* `test_load_document_relations.py` - Unit testy pro `src/tools/load_document_relations.py` (Krok 1 follow-up #16): `resolve_relations` — spárování obou identifikátorů, nenapárovaný `from`/`to` identifikátor i neznámý `relation_type` jdou do fronty k lidské kontrole (nikdy se nehádá). Čisté, bez připojení k databázi.
* `test_parse_v02_processes.py` - Unit testy pro `src/tools/parse_v02_processes.py` (Krok 3a): rozpoznání nadpisu uzlu, `extract_list_items` (upřednostnění stylu `List Paragraph`, fallback na `Normal` odstavce bez úvodní věty končící dvojtečkou), `split_branch_heading` (vč. průřezové větve a variant pomlčky), detekce Krok-větvení a průřezových nadpisů, `split_problem_text`, `parse_subject_cell`, `parse_bibliography_line`, `extract_citations` (české zákony, nařízení vlády, nařízení EU v závorce i bez ní).
* `test_load_process_layer.py` - Unit testy pro `src/tools/load_process_layer.py`: `normalize_citation_core`/`document_identifier_digit_core`/`build_document_lookup`/`match_citation`, vč. regresního testu, že se slovenská STN a česká ČSN adopce stejného čísla normy nikdy nesplynou v citační shodě.
* `test_link_document_relations_auto.py` - Unit testy pro `src/tools/link_document_relations_auto.py` (R1.3/R1.4, 2026-09-11): `international_core` (strhává národní prefix i mezivrstvý "EN " před ISO/IEC, ale ponechá holé "EN 1717" nedotčené, bez kolize různých čísel dílů), `jurisdikce_tier`, `find_localization_pairs` (skupina se propojí jen s aspoň jedním mezinárodním/EU rodičem A aspoň jedním národním dítětem; víc mezinárodních vydání = víc hran; dvě mezinárodní nebo dvě národní samy o sobě nelinkují), `digit_core`, `is_eu_act_znacka` a `find_eu_transposition_pairs` — vč. regresního testu, že EU akt citující svého rodiče (`"(EU) 2023/1184"` → `"(EU) 2018/2001"`) NEVYTVOŘÍ hranu (rozlišeno podle tvaru VLASTNÍ značky citujícího, ne podle `jurisdikce`, protože ta je v korpusu prázdná jak u národních zákonů, tak u EU aktů), že národní zákon s prázdnou `jurisdikce` (reálný stav zdrojů Sinay_Zakony/Haltuf_Dokumenty) přesto linkuje správně, že záznam bez použitelné vlastní značky nikdy neprodukuje hranu ani kandidáta, a že sebe-citace na začátku textu neschová pozdější skutečně odlišnou citaci ve stejném poli (`finditer`, ne jen první shoda).
